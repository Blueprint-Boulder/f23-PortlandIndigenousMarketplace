import React from 'react';
export default function FooterPad() {
  return (
    <div className="w-full h-12">
      {/* This is padding so the footer doesn't hide vital information */}
    </div>
  );
}
